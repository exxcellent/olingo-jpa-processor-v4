name: Deploy OData-JPA-Adapter (javax) on packagecloud
on:
  workflow_dispatch:
  
jobs:
  deployment:
    environment: production
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: 8
          distribution: 'temurin'
          architecture: x64
          cache: maven
      - name: Create code coverage report
        run: mvn clean verify -Duse-eclipselink -Ddisable.jetty=false -Dmaven.source.skip -Dmaven.javadoc.skip
      - uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: code-coverage-report/target/jacoco-aggregate-ut/jacoco.xml
          flags: unittests
#          name: codecov-umbrella
          fail_ci_if_error: true
          verbose: true
      - name: Archive code coverage results for download
        uses: actions/upload-artifact@v3
        with:
          name: code-coverage-report
          path: code-coverage-report/target/jacoco-aggregate-ut/jacoco.xml
#      - name: Upload code coverage report
#        uses: actions/github-script@v6
#        with:
#          script: bash <(curl -s https://codecov.io/bash) -X gcov -Z -F unittests -f code-coverage-report/target/jacoco-aggregate-ut/jacoco.xml
#      - name: Build with Maven
#        run: mvn -s .github/workflows/settings.xml clean package deploy:deploy -DskipTests -Djetty.skip
